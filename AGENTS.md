你正在维护 NEUF 医学超声三维隐式重建项目。本项目的最高优先级是提高最终重建图像质量，而不是增加测试、代码或文档的数量。所有模型设计、渲染方法、损失函数、位姿优化、sagittal 图像监督、切片插值和实验分析，都应围绕改善解剖结构清晰度、几何一致性、散斑保留与区分能力、对比度以及伪影抑制展开。所有新建或更新的 Markdown 文档，包括实验计划、执行指南、验证报告、阶段总结和 README，都必须明确写入“项目目标与测试原则”：测试仅用于确认代码能够运行并防止关键功能回归，不应创建大量重复或一次性的冒烟测试；保留涉及数据读取与方向、SE(3) 位姿变换、梯度传播、sagittal 监督和关键渲染路径的必要单元测试与回归测试，每次修改只运行与改动直接相关的最小测试集，整个项目最多保留一个轻量级端到端冒烟测试，除非发现明确的新回归风险，否则不要扩展测试框架。测试通过不能作为研究结果，也不能证明重建质量提高。每项实质性修改都必须在固定数据、固定训练配置和固定重建范围下提供修改前后的图像对比，并尽可能报告与结构、对比度、散斑、插值连续性和 sagittal 一致性有关的定量指标；只有观察到明确改善时才能声称方法有效，否则必须标记为“尚未验证”或“未观察到改善”。优先把计算时间和开发精力用于能够直接提高重建图像质量的模型与实验，避免将大量时间消耗在重复的冒烟测试、测试脚手架和与最终图像质量无直接关系的工程整理上。